spring:
  profiles:
    # 环境变量
    active: dev
  cloud:
    nacos:
      # 注册配/置地址
      server-addr: ${NACOS_ADDR:192.168.204.177:8848}
      config:
        # 配置文件后缀
        file-extension: yml
        # 公共配置文件
        shared-dataids: ocean-common.yml
jasypt:
  encryptor:
    bootstrap: false