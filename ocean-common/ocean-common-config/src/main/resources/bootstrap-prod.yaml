spring:
  cloud:
    nacos:
      # Nacos发现与配置地址
      server-addr: ${NACOS_ADDR:127.0.0.1:8848}
      config:
        # Nacos配置地址
        namespace: ${NACOS_NAMESPACE:prod}
      discovery:
        # Nacos注册地址
        namespace: ${NACOS_NAMESPACE:prod}