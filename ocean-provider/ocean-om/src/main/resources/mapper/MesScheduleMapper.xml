<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fantechs.provider.om.mapper.MesScheduleMapper">
    <resultMap id="BaseResultMap" type="com.fantechs.common.base.general.entity.om.MesSchedule">
        <result column="schedule_id" jdbcType="BIGINT" property="scheduleId"/>
        <result column="organization_id" jdbcType="BIGINT" property="organizationId"/>
        <result column="schedule_code" jdbcType="VARCHAR" property="scheduleCode"/>
        <result column="total" jdbcType="DECIMAL" property="total"/>
        <result column="finished" jdbcType="DECIMAL" property="finished"/>
        <result column="status" jdbcType="TINYINT" property="status"/>
        <result column="is_delete" jdbcType="TINYINT" property="isDelete"/>
        <result column="remark" jdbcType="VARCHAR" property="remark"/>
        <result column="create_user_id" jdbcType="BIGINT" property="createUserId"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="modified_user_id" jdbcType="BIGINT" property="modifiedUserId"/>
        <result column="modified_time" jdbcType="TIMESTAMP" property="modifiedTime"/>
    </resultMap>

    <resultMap id="BaseResultMapDTO" type="com.fantechs.common.base.general.dto.om.MesScheduleDTO" extends="BaseResultMap">

    </resultMap>

    <select id="selectUserName" resultType="string">
        select su.user_name
        from ocean.sys_user su
        where su.user_id = #{id}
    </select>

    <select id="selectFilterAll" resultMap="BaseResultMapDTO" parameterType="map">
        SELECT
        ms.schedule_id,
        ms.organization_id,
        ms.schedule_code,
        ms.pro_line_id,
        ms.total,
        ms.finished,
        ms.`status`,
        ms.is_delete,
        ms.remark,
        ms.create_user_id,
        ms.create_time,
        ms.modified_user_id,
        ms.modified_time,
        su1.user_name AS 'createUserName',
        su2.user_name AS 'modifiedUserName',
        spl.pro_line_id AS 'proLineId',
        spl.pro_name AS 'proName',
        spl.pro_code AS 'proCode'
        FROM
        fantech_imes.mes_schedule AS ms
        LEFT JOIN ocean.sys_user su1 ON su1.user_id = ms.create_user_id
        LEFT JOIN ocean.sys_user su2 ON su2.user_id = ms.modified_user_id
        LEFT JOIN fantech_imes.smt_pro_line spl ON spl.pro_line_id = ms.pro_line_id
        <where>
            <if test="proLineId != null and proLineId != ''">
                and spl.pro_line_id = #{proLineId}
            </if>
            <if test="scheduleCode != null and scheduleCode != ''">
                and ms.schedule_code = #{scheduleCode}
            </if>
        and ms.is_delete = 1
        </where>
        order by ms.modified_time desc
    </select>

    <insert id="batchScheduleDetail" parameterType="java.util.List">
        insert into fantech_imes.mes_schedule_detail (schedule_id,work_order_id,order_id) values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.scheduleId},#{item.workOrderId},#{item.orderId})
        </foreach>
    </insert>
</mapper>